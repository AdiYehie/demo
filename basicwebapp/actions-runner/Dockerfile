# Use the official Nginx image from Docker Hub
FROM nginx:alpine

# Copy the static files from the local machine to the container
COPY . /usr/share/nginx/html

# Expose port 80 (default HTTP port)
EXPOSE 80

